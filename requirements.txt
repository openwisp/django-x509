# Supports Django 4.2 through 5.2, including release candidates
django>=4.2.0,<=5.3.0
openwisp-utils @ https://github.com/openwisp/openwisp-utils/archive/refs/heads/1.3.tar.gz
# we use mostly the cryptography python library
# but still need pyOpenSSL for limited tasks
# that are not implemented in cryptography
pyopenssl>=25.3.0,<26.0.0
